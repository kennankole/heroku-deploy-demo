version: '3.8'

services:
  web:
    build: ./
    command: gunicorn --bind 0.0.0.0:5000 wsgi:app
    ports:
      - 5000:5000
  
  nginx:
    build: ./app/nginx
    ports:
      - 1337:80
    depends_on:
      - web

